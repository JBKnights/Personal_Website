---
// Placeholder initials avatar. Once you have a real photo, drop it in
// public/images/ and swap this component out for an <img> tag, e.g.:
//   <img src="/images/profile.jpg" alt={`Portrait of ${site.name}`} />
import { site } from "../data/site";
---

<div class="hero-photo">
  <div class="avatar" aria-hidden="true">{site.initials}</div>
</div>
